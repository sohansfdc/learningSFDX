<<<<<<< HEAD
git Hello
=======

git clone - pull the project (data) from the github.
git init - initialize the git from a particular folder.

git cd ../..  - to come back from the folder.
git cd /c/users/sohan.rawat   - navigate the folder (project)
git add firstproject.txt  - add a text file to git hub.

git add -A   --- Add everything to git from the project folder.

git commit -m "use this command to commit the change to git hub file"
git push - push the changes to git hub account

git remote add origin https://github.com/sohansfdc/personal.git  --- add the github repository with your local project to sync.

Added change to git

Git workflow >>

AFter you add or make any change to file or folder run the below commands in sequence.

1-  git add "Git Commands"
2-  git commit -m "updating the file in the staging"
3.  git push "pushing the file to github repository"


++++++++++++++++++++++++++++++++

echo "# learningSFDX" >> README.md
git init
git add README.md
git commit -m "first commit"
git remote add origin https://github.com/sohansfdc/learningSFDX.git
git push -u origin master

+++++++++++++++++++++

Branching
+++++++++++++++++++
[1] git branch
[2] git checkout readMeUpdate  // Switched to branch 'readMeUpdate'
>>>>>>> readMeUpdate
